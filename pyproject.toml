[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling with constrained decoding for small LLMs"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "numpy",
]

[tool.uv]
dev-dependencies = [
    "flake8",
    "mypy",
]